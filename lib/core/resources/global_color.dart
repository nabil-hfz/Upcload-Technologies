import 'package:flutter/material.dart';

class ColorApp {
  final Color accentColor = Color(0XFFffd54f);
  final Color primaryColor = Color(0XFF48494B);
  final Color secondaryColor = Color(0XFF05b746);
  final Color primary = Color(0XFF43a047);
  final Color errorColor = Color(0XFF3C3044);
  final Color scaffoldBackGroundWhiteColor = Colors.white;
  final Color scaffoldBackGroundGreyColor = Colors.grey.shade50;
  final Color appBar = Color(0XFFffffff);

  final Color basic1 = Color(0XFF48494B);
  final Color basic2 = Color(0XFF808588);

  final Color borderColor = Color(0XFF819cc6);
  final Color iconColor = Color(0XFF3b3d55);

  final Color buttonColorPrimary = Color(0XFF2b2d5d);
  final Color buttonColorSecondary = Color(0XFF2966a7);
  final Color buttonColorOrange = Color(0XFFffbe00);

  //general color
  final Color globalWhite = Colors.white;
  final Color globalGrey = Colors.grey;
  final Color globalDarkGrey = Colors.grey.shade600;

  final Color textLabel = Color(0XFF4A4A4A);
  final Color textColor = Color(0XFF201E1f);
  final Color focusedBorder = Color(0XFF48494B);
  final Color enabledBorder = Colors.grey;
  final Color black = Color(0XFF48494B);
}

ColorApp globalColor = ColorApp();
